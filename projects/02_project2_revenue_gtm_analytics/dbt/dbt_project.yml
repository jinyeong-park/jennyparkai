name: 'revenue_intelligence'
version: '1.0.0'
config-version: 2

profile: 'revenue_intelligence'

model-paths: ["models"]
seed-paths:  ["../data/raw"]
test-paths:  ["tests"]

seeds:
  revenue_intelligence:
    +schema: raw

models:
  revenue_intelligence:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts
